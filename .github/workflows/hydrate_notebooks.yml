name: Hydrate Notebooks with Output

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - '**.ipynb'
      - 'src/**'
      - 'requirements.txt'

jobs:
  run-notebooks:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3
        with:
          lfs: true

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install jupyter nbconvert ipykernel
          python -m ipykernel install --user --name venv_final_project

      - name: Execute Japan Analysis
        run: |
          jupyter nbconvert --to notebook --execute --inplace --ExecutePreprocessor.kernel_name=python3 Japan_Analysis.ipynb

      - name: Execute Switzerland Analysis
        run: |
          jupyter nbconvert --to notebook --execute --inplace --ExecutePreprocessor.kernel_name=python3 Switzerland_Analysis.ipynb

      - name: Execute Comparison Analysis
        run: |
          jupyter nbconvert --to notebook --execute --inplace --ExecutePreprocessor.kernel_name=python3 Comparison_Analysis.ipynb

      - name: Execute Japan Construction Story
        run: |
          jupyter nbconvert --to notebook --execute --inplace --ExecutePreprocessor.kernel_name=python3 Graph_Construction_Walkthrough/Japan_Construction_Story.ipynb

      - name: Execute Switzerland Construction Story
        run: |
          jupyter nbconvert --to notebook --execute --inplace --ExecutePreprocessor.kernel_name=python3 Graph_Construction_Walkthrough/Switzerland_Construction_Story.ipynb

      - name: Commit and Push changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add *.ipynb
          # Only commit if there are changes
          git diff --quiet && git diff --staged --quiet || (git commit -m "Hydrate notebooks with outputs [skip ci]" && git push)
